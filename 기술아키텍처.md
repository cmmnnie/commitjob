# CommitJob 프로젝트 기술스택 및 아키텍처

## 1. 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React)                         │
│  Vercel - https://commitjob.site                            │
│                                                              │
│  - React 19.2.0 + Vite 7.1.9                                │
│  - React Router DOM 7.9.3                                   │
│  - Axios (API 통신)                                          │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTPS/API
                       │
┌──────────────────────▼──────────────────────────────────────┐
│              Backend API Server (Node.js)                   │
│     Railway - https://commitjob-backend.up.railway.app      │
│                                                              │
│  - Node.js + Express 5.1.0                                  │
│  - MySQL2 (RDS 연결)                                         │
│  - OpenAI API (GPT-4)                                       │
│  - JWT (jose 6.1.0)                                         │
│  - Swagger UI (API 문서)                                     │
└──────┬───────────────┬──────────────────────────────────────┘
       │               │
       │               └─────────────┐
       │                             │
┌──────▼────────────┐    ┌──────────▼──────────────────────┐
│   MySQL RDS       │    │ Catch Scraper Service (Python)  │
│   (AWS RDS)       │    │  Railway Microservice            │
│                   │    │                                  │
│ - Users           │    │ - Flask 2.x + Flask-CORS        │
│ - User Profiles   │    │ - Selenium WebDriver            │
│ - Jobs            │    │ - SQLAlchemy ORM                │
│ - Companies       │    │ - Headless Chrome               │
│ - Coding Problems │    │ - catch.co.kr 크롤링            │
└───────────────────┘    └─────────────────────────────────┘
```

## 2. Frontend 기술스택

### 2.1 핵심 기술
- **React 19.2.0**: 최신 React (함수형 컴포넌트)
- **Vite 7.1.9**: 빌드 도구 (번들링 및 개발 서버)
- **React Router DOM 7.9.3**: SPA 라우팅
- **Axios 1.12.2**: HTTP 클라이언트

### 2.2 주요 페이지 구조
```
src/pages/
├── MainPage.jsx           # 메인/로그인 화면
├── MenuPage.jsx           # 메뉴 대시보드
├── JobsPage.jsx           # 채용공고 목록 (스크래핑 기능)
├── AIJobsPage.jsx         # BIGDATA/AI 채용공고
├── ITJobsPage.jsx         # IT 채용공고
├── JobDetailPage.jsx      # 채용공고 상세
├── AIRecommendationPage.jsx  # AI 맞춤 채용공고 추천
├── AIInterviewPage.jsx    # AI 면접 질문 생성
├── ResumePage.jsx         # 이력서 관리
└── CallbackPage.jsx       # OAuth 콜백 처리
```

### 2.3 배포 환경
- **플랫폼**: Vercel
- **도메인**: https://commitjob.site (또는 https://commitjob.vercel.app)
- **자동 배포**: GitHub Push → Vercel 자동 빌드/배포
- **빌드 명령어**: `npm run build` (Vite)
- **Root Directory**: `kakao-login-app`
- **Output Directory**: `dist`
- **포트**: 개발 환경 5500

## 3. Backend API 기술스택

### 3.1 핵심 기술
```javascript
{
  "dependencies": {
    "express": "^5.1.0",        // 웹 프레임워크
    "mysql2": "^3.14.4",        // MySQL 드라이버 (Promise 지원)
    "openai": "^6.2.0",         // OpenAI GPT API
    "jose": "^6.1.0",           // JWT 토큰 처리
    "axios": "^1.12.2",         // HTTP 클라이언트
    "cheerio": "^1.1.2",        // HTML 파싱
    "cors": "^2.8.5",           // CORS 처리
    "multer": "^2.0.2",         // 파일 업로드
    "cookie-parser": "^1.4.7",  // 쿠키 파싱
    "swagger-jsdoc": "^6.2.8",  // API 문서 자동 생성
    "swagger-ui-express": "^5.0.1"
  }
}
```

### 3.2 데이터베이스 연결
```javascript
// MySQL Connection Pool
const pool = mysql.createPool({
  host: process.env.DB_HOST,      // AWS RDS 엔드포인트
  port: 3306,
  user: process.env.DB_USER,
  password: process.env.DB_PASS,
  database: process.env.DB_NAME,
  connectionLimit: 10,
  timezone: '+09:00',             // KST
  connectTimeout: 10000,
  waitForConnections: true
});
```

### 3.3 주요 API 엔드포인트
```
인증 (OAuth 2.0)
├── POST /auth/kakao          # 카카오 로그인
├── POST /auth/google         # 구글 로그인
└── POST /auth/logout         # 로그아웃

사용자 프로필
├── GET  /api/profile         # 프로필 조회
├── POST /api/profile         # 프로필 생성/업데이트
└── POST /api/upload-resume   # 이력서 업로드

채용공고
├── GET  /api/jobs            # 전체 채용공고 조회
├── GET  /api/jobs/:category  # 카테고리별 조회 (IT, BIGDATA_AI)
├── POST /api/scrape-jobs     # 채용공고 스크래핑 트리거
└── GET  /api/jobs/detail/:id # 채용공고 상세

AI 기능
├── POST /api/recommend-jobs  # GPT 기반 맞춤 채용공고 추천
└── POST /api/generate-interview # GPT 기반 면접 질문 생성

코딩 테스트
├── GET  /api/coding/problems           # 코딩 문제 목록
└── GET  /api/programmers/problems/:id  # 프로그래머스 문제 상세

API 문서
└── GET  /api/docs            # Swagger UI
```

### 3.4 배포 환경
- **플랫폼**: Railway
- **URL**: https://commitjob-backend.up.railway.app
- **포트**: 4001 (환경 변수로 설정 가능)
- **호스트**: 0.0.0.0 (모든 네트워크 인터페이스)

## 4. Catch Scraper Service (Python Microservice)

### 4.1 핵심 기술
```python
{
  "Flask": "웹 서버",
  "Selenium": "브라우저 자동화",
  "SQLAlchemy": "ORM (MySQL 연동)",
  "Headless Chrome": "브라우저 엔진",
  "BeautifulSoup": "HTML 파싱"
}
```

### 4.2 주요 기능
```python
# Flask 앱 구조
app = Flask(__name__)
CORS(app)

# 주요 엔드포인트
├── POST /scrape-jobs         # 채용공고 스크래핑 시작
├── GET  /scrape-status       # 스크래핑 상태 조회
├── GET  /debug/env           # 환경 설정 확인
└── GET  /debug/chromium      # Chromium 설치 확인
```

### 4.3 웹 스크래핑 프로세스
```python
1. Selenium WebDriver 초기화 (Headless Chrome)
2. catch.co.kr 접속
3. 직무 카테고리 선택
   - IT개발
   - 빅데이터·AI
4. 페이지네이션으로 모든 공고 수집
5. SQLAlchemy로 MySQL에 저장
   - 중복 체크 (UNIQUE 제약)
   - 타임스탬프 기록
```

### 4.4 배포 환경
- **플랫폼**: Railway (별도 서비스)
- **Docker**: Chromium 포함 컨테이너
- **포트**: 3000
- **URL**: 환경 변수 `CATCH_SCRAPER_URL`

### 4.5 Dockerfile 구조
```dockerfile
FROM python:3.11-slim

# Chromium 및 의존성 설치
RUN apt-get update && apt-get install -y \
    chromium \
    chromium-driver \
    ...

# Python 패키지 설치
RUN pip install selenium sqlalchemy flask flask-cors mysql-connector-python

# 애플리케이션 실행
CMD ["python", "catch_scraper.py"]
```

## 5. 데이터베이스 스키마 (MySQL RDS)

### 5.1 주요 테이블
```sql
-- 사용자 정보
users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  email VARCHAR(255),
  name VARCHAR(255),
  picture TEXT,
  provider ENUM('google', 'kakao'),
  created_at TIMESTAMP
)

-- 사용자 프로필
user_profiles (
  user_id INT PRIMARY KEY,
  jobs VARCHAR(255),          # 희망직무
  careers VARCHAR(50),        # 경력
  regions VARCHAR(255),       # 희망지역
  skills JSON,                # 기술스택 배열
  resume_path VARCHAR(500),   # 이력서 경로
  FOREIGN KEY (user_id) REFERENCES users(id)
)

-- 채용공고
jobs (
  id INT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(500),
  company VARCHAR(255),
  category ENUM('IT', 'BIGDATA_AI'),
  url TEXT,
  registration_info JSON,     # 접수기간 등
  scraped_at TIMESTAMP,       # 스크래핑 시간
  UNIQUE KEY (company, title) # 중복 방지
)

-- 코딩 테스트 문제
coding_problems (
  id INT PRIMARY KEY,
  title VARCHAR(255),
  difficulty VARCHAR(50),
  tags JSON,
  companies JSON,
  url TEXT,
  source VARCHAR(50)          # 'baekjoon' or 'programmers'
)
```

## 6. AI/GPT 통합 아키텍처

### 6.1 OpenAI API 사용
```javascript
const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY
});

// GPT-4 사용 (추정)
const response = await openai.chat.completions.create({
  model: "gpt-4",
  messages: [
    { role: "system", content: "시스템 프롬프트" },
    { role: "user", content: "사용자 요청" }
  ]
});
```

### 6.2 AI 추천 시스템 프로세스
```
1. 사용자 프로필 조회
   ├── 희망직무 (jobs)
   ├── 경력 (careers)
   ├── 희망지역 (regions)
   └── 기술스택 (skills)

2. 최근 채용공고 100건 조회
   ├── scraped_at DESC 정렬
   └── 카테고리 필터 (AI/IT)

3. GPT에게 매칭 요청
   ├── 프로필 정보 전달
   ├── 채용공고 데이터 전달
   └── 매칭 알고리즘 실행

4. Top 5 추천 반환
   └── 추천 이유 포함
```

### 6.3 AI 면접 질문 생성 프로세스
```
1. 채용공고 정보 수집
   ├── 회사명
   ├── 직무
   └── 요구 기술

2. 사용자 프로필 분석
   ├── 기술스택
   └── 경력

3. GPT로 면접 질문 생성
   ├── 기술 질문 (3개)
   ├── 경험 질문 (2개)
   └── 직무 관련 질문 (2개)

4. 4단계 프로세스로 UI 표시
   STEP 1: 이력서 조회
   STEP 2: 채용공고 분석
   STEP 3: GPT 질문 생성
   STEP 4: 결과 표시
```

## 7. 인증 시스템 (OAuth 2.0)

### 7.1 소셜 로그인 플로우
```
1. 프론트엔드: 로그인 버튼 클릭
   └── Google or Kakao 선택

2. 백엔드: OAuth URL 생성
   ├── Client ID
   ├── Redirect URI
   ├── State (CSRF 방지)
   └── Scope

3. 소셜 플랫폼: 사용자 인증
   └── Authorization Code 발급

4. 백엔드 Callback: Code → Access Token
   ├── Token 교환
   ├── 사용자 정보 조회
   └── JWT 세션 생성

5. 쿠키 설정
   ├── app_session (JWT)
   ├── HttpOnly
   ├── Secure (HTTPS)
   └── SameSite=None
```

### 7.2 JWT 토큰 구조 (jose 라이브러리)
```javascript
// JWT 생성
const secret = new TextEncoder().encode(process.env.JWT_SECRET);
const token = await new jose.SignJWT({ userId, email })
  .setProtectedHeader({ alg: 'HS256' })
  .setExpirationTime('7d')
  .sign(secret);

// 쿠키 설정
res.cookie('app_session', token, {
  httpOnly: true,
  secure: true,
  sameSite: 'none',
  maxAge: 7 * 24 * 60 * 60 * 1000  // 7일
});
```

## 8. 파일 업로드 시스템

### 8.1 Multer 설정
```javascript
// 이력서 업로드
const resumeStorage = multer.diskStorage({
  destination: './uploads/resume',
  filename: (req, file, cb) => {
    const userId = req.body.user_id;
    const ext = path.extname(file.originalname);
    cb(null, `${userId}_${Date.now()}${ext}`);
  }
});

// 자기소개서 업로드
const coverLetterStorage = multer.diskStorage({
  destination: './uploads/cover-letters',
  filename: (req, file, cb) => {
    cb(null, `cover_letter_${userId}_${timestamp}${ext}`);
  }
});

// 허용 파일 형식
const allowedTypes = ['.pdf', '.doc', '.docx', '.txt'];
const fileSize = 10MB;
```

### 8.2 정적 파일 서빙
```javascript
app.use('/uploads', express.static('./uploads'));
app.use('/uploads/cover-letters', express.static('./uploads/cover-letters'));
```

## 9. CORS 설정

### 9.1 허용된 오리진
```javascript
const allowedOrigins = [
  'https://commitjob.site',
  'https://www.commitjob.site',
  'http://localhost:5173',  // Vite dev
  'http://localhost:5500',  // 커스텀 포트
  'http://localhost:3000',
  'http://localhost:4001'
];

app.use(cors({
  origin: (origin, callback) => {
    if (allowedOrigins.includes(origin) || !origin) {
      callback(null, true);
    } else {
      callback(new Error('CORS blocked'));
    }
  },
  credentials: true  // 쿠키 허용
}));
```

## 10. 배포 환경 및 인프라

### 10.1 프론트엔드 (Vercel)
```
- 자동 빌드/배포: GitHub Push 트리거
- CDN: Vercel Edge Network
- HTTPS: 자동 SSL 인증서
- 도메인: commitjob.site, commitjob.vercel.app
- Framework: Vite
- Root Directory: kakao-login-app
- 빌드 명령어: npm run build
- 출력 디렉토리: dist/
```

### 10.2 백엔드 (Railway)
```
- Node.js 런타임
- 자동 배포: GitHub Push 트리거
- 환경 변수: Railway 대시보드에서 관리
- 포트: $PORT (Railway 자동 할당)
- Health Check: /api/health
```

### 10.3 스크래퍼 (Railway - 별도 서비스)
```
- Docker 컨테이너
- Chromium 포함 이미지
- Python Flask 앱
- MySQL 직접 연결
```

### 10.4 데이터베이스 (AWS RDS MySQL)
```
- Engine: MySQL 8.x
- Region: ap-northeast-2 (서울)
- Instance: (크기는 환경 변수 기반)
- Timezone: Asia/Seoul (UTC+9)
- Connection Pool: 10 connections
```

## 11. 주요 환경 변수

```bash
# Backend (.env)
DB_HOST=<RDS_ENDPOINT>
DB_PORT=3306
DB_USER=admin
DB_PASS=<PASSWORD>
DB_NAME=commitjob

JWT_SECRET=<SECRET_KEY>

GOOGLE_CLIENT_ID=<GOOGLE_OAUTH>
GOOGLE_CLIENT_SECRET=<GOOGLE_SECRET>
KAKAO_CLIENT_ID=<KAKAO_OAUTH>
KAKAO_CLIENT_SECRET=<KAKAO_SECRET>

OPENAI_API_KEY=<GPT_API_KEY>

CATCH_SCRAPER_URL=<SCRAPER_SERVICE_URL>

FRONTEND_ORIGIN=https://commitjob.site
```

## 12. 기술적 특징 및 장점

1. **마이크로서비스 아키텍처**
   - 스크래퍼를 별도 서비스로 분리
   - 각 서비스 독립 배포 가능

2. **GPT 기반 AI 추천**
   - 단순 키워드 매칭이 아닌 GPT-4 기반 지능형 매칭
   - 자연어 추천 이유 제공

3. **실시간 데이터 수집**
   - Selenium 기반 동적 웹 스크래핑
   - catch.co.kr 실시간 채용공고 수집

4. **소셜 로그인**
   - Google + Kakao OAuth 2.0
   - JWT 기반 세션 관리

5. **자동 배포 파이프라인**
   - GitHub → Vercel (Frontend)
   - GitHub → Railway (Backend + Scraper)

6. **API 문서화**
   - Swagger UI 자동 생성
   - JSDoc 기반 스펙 정의

## 13. 프로젝트 구조

```
project/
├── kakao-login-app/           # Frontend (React + Vite)
│   ├── src/
│   │   ├── pages/             # 페이지 컴포넌트
│   │   ├── components/        # 재사용 컴포넌트
│   │   └── App.jsx            # 메인 앱
│   ├── dist/                  # 빌드 결과물
│   ├── package.json
│   └── vite.config.js
│
├── backend/                   # Backend API Server
│   ├── server.js              # Express 서버
│   ├── routes/                # API 라우터
│   │   ├── coding.js
│   │   └── programmers.js
│   ├── scripts/               # 크롤러 스크립트
│   ├── uploads/               # 업로드 파일 저장소
│   ├── package.json
│   └── .env                   # 환경 변수
│
└── backend/catch-scraper-service/  # Python Scraper
    ├── catch_scraper.py       # Flask 앱
    ├── Dockerfile             # Docker 설정
    └── requirements.txt       # Python 패키지
```

## 14. 개발 환경 설정

### 14.1 Frontend 로컬 실행
```bash
cd kakao-login-app
npm install
npm run dev
# http://localhost:5500
```

### 14.2 Backend 로컬 실행
```bash
cd backend
npm install
node server.js
# http://localhost:4001
```

### 14.3 Scraper 로컬 실행
```bash
cd backend/catch-scraper-service
pip install -r requirements.txt
python catch_scraper.py
# http://localhost:3000
```

## 15. API 테스트

### 15.1 Swagger UI
```
https://commitjob-backend.up.railway.app/api/docs
```

### 15.2 주요 테스트 엔드포인트
```bash
# Health Check
curl https://commitjob-backend.up.railway.app/api/health

# 채용공고 조회
curl https://commitjob-backend.up.railway.app/api/jobs

# 카테고리별 조회
curl https://commitjob-backend.up.railway.app/api/jobs/IT
curl https://commitjob-backend.up.railway.app/api/jobs/BIGDATA_AI
```

---

**프로젝트 저장소**: https://github.com/cmmnnie/commitjob
**프로덕션 URL**: https://commitjob.site
**최종 업데이트**: 2025-10-12
