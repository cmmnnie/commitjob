# Railway Build Optimization v2
[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci --omit=dev --prefer-offline --no-audit --no-fund"]

[phases.build]
# Skip build phase - no compilation needed for Node.js
cmds = ["echo 'Build skipped'"]

[start]
cmd = "node server.js"
